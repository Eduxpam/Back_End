CREATE TABLE usuario (
	CODIGO SERIAL PRIMARY KEY NOT NULL,
	NOME VARCHAR(100) NOT NULL,
	USUARIO VARCHAR(20) NOT NULL, 
	SENHA VARCHAR(32) NOT NULL,
	ATIVO VARCHAR(1) DEFAULT 'S' NOT NULL
);

CREATE TABLE produto (
	CODIGO VARCHAR(20) PRIMARY KEY NOT NULL,
	DESCRI VARCHAR(100) NOT NULL,
	PRECO DOUBLE PRECISION NOT NULL,
	QUANT INTEGER NOT NULL,
	IMG VARCHAR(200),
	ATIVO VARCHAR(1) DEFAULT 'S' NOT NULL
);

INSERT INTO public.usuario(nome, usuario, senha, ativo)	VALUES ('ADMIN', 'ADMIN', '21232f297a57a5a743894a0e4a801fc3', 'S');

